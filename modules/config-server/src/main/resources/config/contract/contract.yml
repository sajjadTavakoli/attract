spring:
  application:
    name: contract

server:
  port: 8082

eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8072/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"